/* exported capitalize */
// define a function capitalize witha parameter of (word)
// define a var upperC that sets the first index in the string to be a capital and the rest of the letters to lowecase
// return the upperC variable
function capitalize(word) {
  var upperC = word[0].toUpperCase() + word.substring(1).toLowerCase();
  return upperC;
}

// define a function capitalize witha parameter of (word)
// define a var upperC that sets the first index in the string to be a capital and the rest of the letters to lowecase
// return the upperC variable
