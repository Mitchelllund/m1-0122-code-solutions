/* exported getFirstChar */

// define a function getFirstChar with one parameter of (string)
// define a var firstChar with a value of charAt of object string to get first character
// return the var firstChar

function getFirstChar(string) {
  var firstChar = string.charAt(0);
  return firstChar;
}

// define a function getFirstChar with one parameter of (string)
// define a var firstChar with a value of charAt of object string to get first character
// return the var firstChar
