/* exported getLastChar */
// define a function getLastChar with a parameter (string)
// define a variable named lastChar that targets the last element in the array
// return the lastChar variable

function getLastChar(string) {
  var lastChar = string.charAt(string.length - 1);
  return lastChar;
}
// define a function getLastChar with a parameter (string)
// define a variable named lastChar that targets the last element in the array and returns the value to lastChar
// return lastChar variable
