/* exported isUpperCased */
// define a function named isUpperCased with parameter (word)
// define a variable upperCase that is assigned the value of parameter set to uppercase
// return the variable upperCase
function isUpperCased(word) {
  return word === word.toUpperCase();
}

// define a function named isUpperCased with parameter (word)
// return the word variable which is strictly equal to parameter word set to uppercase
