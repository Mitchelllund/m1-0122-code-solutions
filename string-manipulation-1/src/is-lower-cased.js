/* exported isLowerCased */
// define a function named islowerCased with parameter (word)
// define a variable lowerCase that is assigned the value of parameter set to lowercase
// return the variable lowerCase
function isLowerCased(word) {
  return word === word.toLowerCase();
}

// define a function named isLowerCased with parameter (word)
// return word which is strictly equal to parameter (word) set to lowercase
